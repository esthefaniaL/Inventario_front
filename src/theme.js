// src/theme.js
import { createMuiTheme } from '@material-ui/core/styles';

// theme.js
const theme = createMuiTheme({
    palette: {
      background: {
        main: '#f0f0f0', // Color de fondo personalizado
      },
    },
  });
export default theme;